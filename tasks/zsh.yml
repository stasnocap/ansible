- name: Install ZSH
  apt:
    name: zsh
    state: present
  become: true
  tags: &tags_for_zsh_tasks [ 'dotfiles', 'zsh', 'wsl', 'home' ]

- name: Change shell to zsh
  user:
    name: "stasnocap"
    shell: /usr/bin/zsh  # Change this to the absolute path of your zsh binary
  become: true
  tags: *tags_for_zsh_tasks

- name: Check that the somefile.conf exists
  stat:
    path: /home/stasnocap/.oh-my-zsh
  register: oh_my_stats
  tags: *tags_for_zsh_tasks

- name: Oh-My-Zsh
  shell: curl -L https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh > ~/.oh-my-installer && chmod +x ~/.oh-my-installer && ~/.oh-my-installer
  tags: *tags_for_zsh_tasks
  when: oh_my_stats.stat.exists == False
  become_user: stasnocap

- name: Install zsh-autosuggestions
  ansible.builtin.git:
    repo: 'https://github.com/zsh-users/zsh-autosuggestions.git'
    dest: "~/.oh-my-zsh/plugins/zsh-autosuggestions"
  tags: *tags_for_zsh_tasks

- name: Remove autogenerated .zshrc file
  file:
    path: "{{ lookup('env', 'HOME') }}/.zshrc"
    state: absent
  tags: *tags_for_zsh_tasks
